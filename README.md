<h1 align="center">FPSCounter</h1>
This small ModuleScript gets a **stable** FPS count by averaging the last `sampleSize` rendering durations (20 by default).

#### API
`FPSCounter.GetFPS(): number`.
